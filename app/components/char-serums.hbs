<!-- <div id="serums-tab" class="tab-pane fade"> -->

<b>Revitalizer Serums: </b>{{{this.char.custom.v_serums}}} <br />

<b>Adreno Serums: </b>{{{this.char.custom.a_serums}}} <br />

<b>Quickhand Serums: </b>{{{this.char.custom.qh_serums}}} <br />

<b>Glass Cannon Serums: </b>{{{this.char.custom.gc_serums}}} <br />

<b>Hardy Serums: </b>{{{this.char.custom.h_serums}}} <br />

<!-- <div class="action-buttons">
    <button class="btn btn-primary" {{action (mut this.showGetSerum) true}}>Get a Serum</button>
</div> -->

<!--<button class="btn btn-secondary" id="addbg" {{on 'click' (fn this.setSelectBackgroundSkill true)}}>Add Background Skill</button> -->
<button class="btn btn-secondary" id="getserum" {{on 'click' (fn this.setSelectSerum true)}}>Get a Serum</button> 
<TooltipButton @onClick={{(fn this.setShowNewConversation true)}} @position="right" @message="New Conversation" @icon="fa fa-plus-circle" />
<!--<BsModalSimple @title="Get a Serum" @closeTitle="Cancel" @submitTitle="Get" @size={{null}} @fade={{true}} @open={{this.showGetSerum}} @onSubmit={{this.getSerum}} @onHide={{(fn this.setShowGetSerum false)}}>
  
  <b>Serum</b> <Input @type="text" size={{25}} id="serumType" @enter={{this.getSerum}} @value={{this.serumType}} />

  </BsModalSimple>--->

<BsModalSimple 
  @title="Get a Serum"
  @closeTitle="Cancel"
  @submitTitle="Get Serum"
  @size=""
  @closeButton={{true}}
  @fade={{true}}
  @backdrop={{true}}
  @backdropClose={{true}}
  @open={{true}}
  @onSubmit={{this.getSerum}} 
  @onHide={{(fn this.setSelectSerum false)}}
  @renderInPlace={{true}}>
    Enter a serum name.
</BsModalSimple>

<BsModalSimple @title="Start Conversation" @closeTitle="Cancel" @submitTitle="Start" @size={{null}} @fade={{true}} @open={{this.showNewConversation}} @onSubmit={{this.startConversation}}  @onHide={{(fn this.setShowNewConversation false)}}>


         <div class="card card-default">
           <div class="card-body">
             
             
              <div class="form-item">
               <div class="form-item-label">
                   To:
               </div>
               <div class="form-item-entry">
                   <PowerSelectMultiple @selected={{this.newConversationList}} @options={{this.model.characters}} @searchEnabled=true  @searchField="name" @onChange={{this.conversationListChanged}} as |char|>
                 {{char.name}}
               </PowerSelectMultiple>
               </div> 
             </div>
             
              <div class="form-item">
               <div class="form-item-label">
                   From:
               </div>
               <div class="form-item-entry">
                 <div class="scene-char-select" >
                   <PowerSelect @selected={{this.poseChar}} @options={{this.model.chat.pose_chars}} @searchField="name" @onChange={{this.poseCharChanged}} as |poseChar|>
                       <CharIconTiny @char={{poseChar}} />
                   </PowerSelect>
                 </div>
               </div> 
             </div>
             
      
               <MarkdownEditor @text={{this.newPage}} class="chat-box" @rows="6" @onEnter={{this.startConversation}} />
      
           </div>
         </div>
         
         <FlashMessages />
  </BsModalSimple>

<!--</BsModalSimple>

<BsModalSimple @title="Add Background Skill" @closeTitle="Cancel" @submitTitle="Add" @size={{null}} @fade={{true}} @open={{this.selectBackgroundSkill}} @onSubmit={{this.addBackgroundSkill}} @onHide={{(fn this.setSelectBackgroundSkill false)}}>
    <p>Enter a skill name.</p>
    <Input @type="text" size={{25}} id="bgSkill" @enter={{this.addBackgroundSkill}} @value={{this.newBgSkill}} />

</BsModalSimple> -->

<!--
<BsModalSimple @title="Spend a Luck Point" @closeTitle="Cancel" @submitTitle="Spend" @size={{null}} @fade={{true}} @open={{this.selectSpendLuck}} @onSubmit={{this.spendLuck}} @onHide={{(fn this.setSelectSpendLuck false)}}>
  
  <b>Reason:</b> <Input @type="text" size={{25}} id="luckReason" @enter={{this.spendLuck}} @value={{this.luckReason}} />

</BsModalSimple> -->



