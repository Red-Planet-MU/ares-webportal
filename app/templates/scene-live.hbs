<style>
  @media screen and (max-width: 1024px) {
	  .body-wrap {
		  display: block!important;
	  }
  }

  @media screen and (min-width: 1001px) and (max-width: 1470px) {
	  .body-wrap {
		  grid-template-columns: minmax(70%, 2150px) auto;
	  }
  }
</style>
{{page-title this.pageTitle}}

 
<h1>Scene {{this.model.scene.id}}
    {{#if this.model.scene.title}}
     - {{this.model.scene.title}}
     {{/if}}
 </h1>
    
     
      <LiveSceneControl @scene={{this.model.scene}} @locations={{this.model.locations}} @abilities={{this.model.abilities}} @places={{this.model.scene.places}} @playerName={{this.currentUser.name}} @characters={{this.model.characters}} @scrollPaused={{this.scrollPaused}} @custom={{this.model.custom}} @refresh={{this.refresh}} @onScrollDown={{this.scrollScene}} @onSetScroll={{this.setScroll}} />


{{outlet}}