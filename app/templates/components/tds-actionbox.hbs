<div class="row tds-abilitybox"> 
  <div class="col col-sm-7">
    {{yield}}
  </div>

  <div class="col col-sm-5">
    <div class="tds-rating-control">
      <button class="tds-skillbutton" {{action "decrement"}}>-</button>

      <div class="tds-rating-display">
        <TdSDots @rating={{this.rating}} @max={{8}} />
        <br>
        {{this.ratingName}}
      </div>

      <button class="tds-skillbutton" {{action "increment"}}>+</button>
    </div>
    
  </div>
  

  {{#if this.specialties}}
    <div class="tds-specialty-select">

      {{#if (gt this.rating 2)}}
      <div class="cg-tip">Specialty is required for ratings 3+.</div>
      {{else}}
      <div class="cg-tip">Specialty is optional for ratings 1-2.</div>
      {{/if}}
       
        {{#each this.specialties as |spec|}}
      
            <span class="text">&nbsp; {{spec.name}}:</span>
            <Input @type="checkbox" @checked={{spec.selected}} /> 
        {{/each}}
    </div>
  {{/if}}
  
</div>
